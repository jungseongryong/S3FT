# ASFT dependencies with a modern vLLM stack.
# The original requirements.txt pins vllm==0.6.3.post1, which is too old
# for the current CUDA/Python stack used in this workspace.

# core deps
transformers>=4.49.0,<=4.57.1,!=4.52.0,!=4.57.0
datasets>=2.16.0,<=4.0.0
accelerate>=1.3.0,<=1.11.0
peft>=0.14.0,<=0.17.1
trl>=0.8.6,<=0.9.6

# gui / plotting
gradio>=4.38.0,<=5.45.0
matplotlib>=3.7.0
tyro<0.9.0

# ops
einops
numpy<2.0.0
pandas>=2.0.0
scipy

# model and tokenizer
sentencepiece
tiktoken
modelscope>=1.14.0
hf-transfer
safetensors<=0.5.3

# python
fire
omegaconf
packaging
protobuf
pyyaml
pydantic>=2.11.7,<3

# api
uvicorn
fastapi
sse-starlette

# media
av
librosa

# yanked
propcache!=0.4.0

# eval
tqdm
python_dateutil
pyyaml>=5.1
sympy==1.12
word2number
Pebble
timeout-decorator
openai>=1.5.0
patsy
scikit-learn

# modern inference engine
vllm==0.11.0
